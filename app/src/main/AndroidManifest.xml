<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gheinrich.cocc_mobile">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.example.test.android.MAPS_RECEIVE"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_GPS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".google_map"
            android:label="@string/title_activity_google_map">

        </activity>
        <!-- here is a list of all of my activities in the application ************ -->

        ******     <activity android:name=".MyActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>            ******
        ******     <activity android:name=".web_view_login"></activity>        ******

        ******    <activity android:name=".library"></activity>                ******

        ******    <activity android:name=".facebook_cocc"></activity>          ******

        ******    <activity android:name=".LibTwitButton_webview"></activity>  ******

        ******    <activity android:name=".LibraryFbButton_webview"></activity>  ****

        ******    <activity android:name=".YouTube"></activity>               *******

        ******    <activity android:name=".Directory"></activity>             *******

        ******    <activity android:name=".test_file"></activity>             ********

        ******   <activity android:name=".blackboard"></activity>             *********

        ******   <activity android:name=".emoloyee_webview"></activity>       *********

        ******   <activity android:name=".real_library"></activity>
        **********
        ******   <activity android:name=".XMPP_Chat"></activity>       *********
        ****** **********

        <!-- ************************************************************** -->

    </application>

</manifest>
